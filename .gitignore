# Rust build artifacts
/target
target/icon-build

# Packaged app / disk image — these ship as release assets ONLY, never in the repo
/dist
*.app
*.dmg

# Xcode / build cruft (kept for completeness)
build/
DerivedData/
*.xcuserstate

# macOS
.DS_Store
